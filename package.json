{
  "name": "nata-finance",
  "version": "1.0.0",
  "description": "Cross-border native USDC payments on Arc testnet.",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:arc": "hardhat run scripts/deploy.js --network arcTestnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17"
  }
}
